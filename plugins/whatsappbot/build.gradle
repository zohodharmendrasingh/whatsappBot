// WhatsApp Bot plugin. PostgreSQL JDBC driver is pulled in here so the whole
// OFBiz instance can run on PostgreSQL without editing the root build.gradle.
dependencies {
    pluginLibsRuntime 'org.postgresql:postgresql:42.7.7'
}
